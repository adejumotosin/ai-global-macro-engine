# Data policy
Raw third-party histories are not committed by default. Macro data are fetched from FRED. Market prices are fetched through an isolated Yahoo chart adapter for the MVP. For institutional research, replace the market adapter with a licensed total-return or futures data source and use ALFRED vintages for point-in-time macro backtests.
